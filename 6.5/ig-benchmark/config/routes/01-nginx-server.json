{
  "name" : "01-nginx-server",
  "baseURI" : "web-lee:80",
  "condition" : "${matches(request.uri.path, '^/home')}",
  "handler" : {
    "type" : "Chain",
    "config" : {
      "handler": {
        "type": "ClientHandler",
        "comment": "Log request, pass it to the sample nginx app, log response",
        "capture": "all",
        "baseURI": "web-lee:80"
      }
    }
  }
}