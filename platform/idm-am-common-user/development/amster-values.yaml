  domain: forgeops.com
  amadminPassword: password
  userStore:
    host: userstore-0.userstore
  resources:
    requests:
      memory: "64Mi"
  config:
    strategy: files
    importPath: /opt/amster/config/
